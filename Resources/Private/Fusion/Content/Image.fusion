prototype(Kleisli.Kubbar:Content.Image) < prototype(Neos.Neos:ContentComponent) {

    class = ''
    image = ${node.properties.image}

    renderer = Kleisli.Kubbar:Component.Molecule.Figure {
        class = ${props.class}
        image.asset = ${props.image}
    }
}
